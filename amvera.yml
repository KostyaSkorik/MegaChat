---
version: null
meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 17
build:
  image: image
  args: spring-boot:repackage
  artifacts:
    build/libs/*.jar: /
run:
  image: image
  jarName: MegaChat-0.0.1-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: 8080
